import React from 'react';
import reactDOM from 'react-dom';
import App from './components/app.component';
reactDOM.render(
    <App />, document.getElementById('root')
)
// index.js is main file to render content in DOM
// Now we create components and to display things inside in DOM through this index.js


// We import react and reactdom here from react and react-dom
